# Required for /api/admin login (portfolio admin UI at client route /admin).
# Copy to server/.env and restart the API. Paths load from this file even if you start Node from the repo root.
ADMIN_PASSWORD=choose-a-long-random-password
ADMIN_SESSION_SECRET=another-long-random-secret-for-signing-tokens
